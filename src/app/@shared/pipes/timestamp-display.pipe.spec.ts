import { TimestampDisplayPipe } from './timestamp-display.pipe';

describe('TimestampDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimestampDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
